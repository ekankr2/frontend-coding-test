const CardComponent = () => {
    return (
        <div className="card">
            <button onClick={() => console.log("hello")}>hello</button>
        </div>
    );
};

export default CardComponent;
